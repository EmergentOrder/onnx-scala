name: Scala CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: ubuntu-latest
    strategy:
      matrix:
        java: [ '17', '21', '25' ]
    steps:
    - uses: actions/checkout@v7
    - uses: actions/setup-node@v7
      with:
        node-version: 24
    - name: Set up Java
      uses: actions/setup-java@v5
      with:
        distribution: 'adopt'
        java-version: ${{ matrix.java }}
    - uses: sbt/setup-sbt@v1
    - name: Run JVM tests
      run: sbt "backends / testFull"
